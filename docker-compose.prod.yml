# PLANNING.md Slice 6 (AD-30..AD-35, VPS revision). A merge overlay on top of docker-compose.yml —
# not a standalone copy — because a plain VPS needs no build:/image:/networking translation (AD-31):
# `docker compose -f docker-compose.yml -f docker-compose.prod.yml --env-file .env.prod up -d --build`
#
# Every `volumes: !override [...]` below exists because Compose's default list-merge is
# *concatenation*, not replacement — confirmed empirically (not assumed) with `docker compose config`
# before this file was written: an empty `volumes: []` override left docker-compose.yml's dev bind
# mounts fully intact. `!override` (Compose Specification's merge-control tag) is what actually
# replaces the list. Without this, the dev source bind-mount would shadow the built prod image's own
# code, and producer's `./data:/data:ro` bind mount would shadow the NAB CSVs baked into the backend
# prod image at build time (backend/Dockerfile), silently reintroducing the exact "no NAB data on the
# server" bug the Fly.io attempt hit (PLANNING §7). Requires the `!override` tag's minimum Compose
# version — confirmed present on the actual Hetzner marketplace image's bundled Compose before this
# file was relied on for a real deploy (PLANNING §7).

x-prod-build: &prod-build
  target: prod

services:
  backend:
    build: *prod-build
    volumes: !override
      - ./models:/app/models:ro
    environment:
      CORS_ALLOWED_ORIGINS: https://${PUBLIC_HOSTNAME}

  migrate:
    build: *prod-build

  producer:
    build: *prod-build
    volumes: !override []
    environment:
      REPLAY_SPEED: "3600"

  consumer:
    build: *prod-build

  # /code-review high fix (PR #7): kept separate from `backend` rather than giving the persistent,
  # internet-facing `backend` service read-write access to /app/models just so this one-shot command
  # could write to it — `backend` only ever needs to read the model it loads at startup (AD-24).
  train:
    build:
      context: ./backend
      <<: *prod-build
    command: ["python", "scripts/train.py"]
    # `train` has no entry in docker-compose.yml to inherit environment from (it's new, prod-only) —
    # docker-compose.yml's `x-backend-env` anchor is scoped to that file and can't be referenced
    # across files, so these are spelled out here rather than aliased (same values, same convention).
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-app}:${POSTGRES_PASSWORD}@timescaledb:5432/${POSTGRES_DB:-monitoring}
      KAFKA_BOOTSTRAP_SERVERS: redpanda:9092
      MODEL_PATH: /app/models/isolation_forest.joblib
    volumes: !override
      - ./models:/app/models
    profiles: ["tools"]

  frontend:
    build:
      <<: *prod-build
      args:
        NEXT_PUBLIC_API_URL: https://${PUBLIC_HOSTNAME}
    volumes: !override []

  # AD-34: the one public entry point. Unlike the abandoned Fly version, there is no edge proxy in
  # front of this — Caddy terminates its own TLS via its built-in automatic HTTPS (Let's Encrypt),
  # which needs ports 80 (ACME HTTP-01 challenge + HTTP->HTTPS redirect) and 443 published for real,
  # not just an internal port. Reaches backend/frontend by compose's own service-name DNS
  # (`backend:8000`, `frontend:3000`) — a plain VPS never needed Fly's shared-namespace `localhost`
  # addressing in the first place (AD-31).
  caddy:
    image: caddy:2-alpine
    volumes:
      - ./deploy/vps/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    environment:
      PUBLIC_HOSTNAME: ${PUBLIC_HOSTNAME}
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      backend:
        condition: service_healthy
      frontend:
        condition: service_healthy

volumes:
  caddy_data:
  caddy_config:
