# Copy to .env for local development. Never commit the real .env (already gitignored).

# TimescaleDB
POSTGRES_USER=app
POSTGRES_PASSWORD=app
POSTGRES_DB=monitoring
POSTGRES_PORT=5432

# Redpanda (Kafka external listener, for host-side tools like rpk or the producer script)
REDPANDA_EXTERNAL_PORT=19092

# Backend / frontend host ports
BACKEND_PORT=8000
FRONTEND_PORT=3000

# NAB replay pacing: 0 = as fast as Kafka accepts (default), N = 1 simulated hour per N/3600 real
# seconds (e.g. 60 = 1 simulated hour per real minute)
REPLAY_SPEED=0
